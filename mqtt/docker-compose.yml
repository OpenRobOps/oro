services:
  mosquitto:
    image: iegomez/mosquitto-go-auth:latest
    # Use host network so Mosquitto can reach Meteor's MongoDB on localhost:3001
    network_mode: host
    volumes:
      - ./mosquitto/mosquitto.conf:/etc/mosquitto/mosquitto.conf
    restart: unless-stopped
