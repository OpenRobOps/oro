/**
 * Dashboard Meteor dependant component
 */
import React from 'react';
import DashboardComponent from './Dashboard_index';

const Dashboard = (props) => (
  <DashboardComponent {...props} />
);

export default Dashboard;
