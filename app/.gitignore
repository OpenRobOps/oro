node_modules/

# Meteor settings (real credentials) — keep settings-example.json tracked
settings*.json
!settings-example.json

# Meteor Modern-Tools build context directories
_build
*/build-assets
*/build-chunks
.rsdoctor

# Unit test results. Normally ran on Github actions, but ignored anyway
unit_server.xml
